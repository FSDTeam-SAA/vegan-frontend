import { OrganizationEvent } from "@/types/organization";

interface Props {
  data: OrganizationEvent;
}

const ViewAttendees = ({}: Props) => {
  return <div>ViewAttendees</div>;
};

export default ViewAttendees;
