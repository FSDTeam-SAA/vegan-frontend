"use client";
import { ReactNode } from "react";

const PlaxibleAnalyticsProvider = ({ children }: { children: ReactNode }) => {
  return <div>PlaxibleAnalyticsProvider</div>;
};

export default PlaxibleAnalyticsProvider;
